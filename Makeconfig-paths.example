#--*- Makefile -*--

# In this Makefile, you can configure your specic PATH and other
# variables.

# 'make prove' is an extra target in the subrojects of this directory
# that should run 'gnatprove' on SPARK sources:

EXTRA_TARGETS ?= prove run

# Add paths specific to this project (e.g. the local 'tools'
# directory) *and* to your build host to this variable:

PATH := ${PATH}:tools

export PATH
