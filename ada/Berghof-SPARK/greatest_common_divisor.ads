package Greatest_Common_Divisor with Spark_Mode
is
   --# function Gcd (A, B : Natural) return Natural;
   procedure G_C_D (M, N : in Natural; G : out Natural);
   --# derives G from M, N;
   --# post G = Gcd (M, N);
end Greatest_Common_Divisor;
